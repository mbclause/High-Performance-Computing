exercise_1.cu - Usage: ./a.out
A CUDA program which initializes an array array[] of size 64 to all 2s in parallel.

exercise_2.cu - Usage: .a/.out
Same as exercise_1 but the array has size 1024.

exercise_3.cu - Usage: ./a.out
Same as exercise_2, but also creates another kernel that adds i to 
array[i].

exercise_4.cu - Usage: ./a.out
Same as exercise_1, but changes the array size to 8000.